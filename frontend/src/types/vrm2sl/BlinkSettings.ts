/** Blink behavior configuration. */
export interface BlinkSettings {
  /** Enables blink animation. */
  enabled: boolean;
  /** Blink interval in seconds. */
  interval_sec: number;
  /** Blink close duration in seconds. */
  close_duration_sec: number;
  /** Enables wink behavior. */
  wink_enabled: boolean;
}
