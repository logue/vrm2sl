/** Texture dimension metadata. */
export interface TextureInfo {
  /** Texture index in glTF image list. */
  index: number;
  /** Texture width in pixels. */
  width: number;
  /** Texture height in pixels. */
  height: number;
}
