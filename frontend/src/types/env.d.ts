/**
 * Global compile-time constants defined in app/vite.config.ts
 * These are replaced at build time with values from the root .env file.
 */

/**
 * Application display name (from APP_NAME).
 */
declare const __APP_NAME__: string;

/**
 * Application version (from VERSION).
 */
declare const __APP_VERSION__: string;
