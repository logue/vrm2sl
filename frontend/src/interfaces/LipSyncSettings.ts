/** Lip sync behavior configuration. */
export interface LipSyncSettings {
  /** Enables lip sync behavior. */
  enabled: boolean;
  /** Lip sync mode. */
  mode: string;
  /** Mouth open angle factor. */
  open_angle: number;
  /** Lip sync speed factor. */
  speed: number;
}
