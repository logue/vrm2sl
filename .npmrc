shamefully-hoist=true
public-hoist-pattern[]=@vuetify/*

# Dev-only dependencies have known vulnerabilities from upstream packages
# (@nuxtjs/sitemap, @modyfi/vite-plugin-yaml). They are NOT included in production builds.
# Overrides in pnpm-workspace.yaml attempt to mitigate. Set audit-level to suppress warnings.
audit-level=moderate
